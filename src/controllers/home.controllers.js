function homePingController(req, res) {
  // collect the request
  // send it to service
  // then prepare and send response
  return res.json({ msg: "Hello welcome" });
}
module.exports = homePingController;
